<UserControl x:Class="MethodistApplication.Views.Users"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MethodistApplication.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="189*"/>
            <ColumnDefinition Width="216*"/>
            <ColumnDefinition Width="395*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="47*"/>
            <RowDefinition Height="403*"/>
        </Grid.RowDefinitions>
        <DataGrid Name="UsersTable"  
                  BorderBrush="#FF258DF3"
                  IsReadOnly="True" SelectionMode="Single"
                  ItemsSource="{Binding}" Grid.Row="1" Grid.ColumnSpan="3"
                  >

        </DataGrid>
        <Button Name="NewUserButton" Content="+Новый пользователь" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="NewUserButton_Click"/>

    </Grid>
</UserControl>
